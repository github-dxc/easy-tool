# easy-tool v0.2.0

一个基于 Rust + Slint 的 Windows 桌面效率工具，提供本地化与云端两种模式的图像识别与文本处理能力。

## 主要功能

### 时间戳转换
- 复制时间戳后自动弹出转换窗口
- 支持多个常用时区

### 剪贴板历史
- 记录文本剪贴板内容
- 支持快速查看与复用历史条目

### 截图工具
- 支持快捷键打开截图窗口
- 提供基础标注功能（矩形、椭圆、箭头、画笔、马赛克、文字）

### 图片预览
- 可从主界面或系统右键菜单打开图片预览
- 侧边栏一键展开翻译面板
- 支持本地 OCR 和腾讯云 OCR 两种识别后端

### 文本翻译
- 支持配置本地 ONNX 翻译模型
- 支持腾讯云 TMT 机器翻译 API
- 启用后复制触发翻译

### Base64 转换
- 支持文本 Base64 编码与解码
- 输入即时转换，支持一键切换编解码模式

### 系统托盘常驻
- 通过托盘菜单快速切换功能和打开主界面
- 左键点击托盘图标居中打开主界面

## 技术栈

- Rust 2024
- Slint
- ONNX Runtime
- tokenizers
- rdev
- tray-icon
- windows-sys
- Noto Sans 字体（Unicode 全字符覆盖）

## 安装与使用

### 运行项目

```bash
cargo run
```

### 构建发布版本

```bash
cargo build --release
```

产物位置：`target/release/easy-tool.exe`

### 首次启动

程序会在 `%APPDATA%/easy-tool/config.toml` 创建配置文件。

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl + C | 复制后触发时间戳转换 / 翻译 |
| Ctrl + Shift + C | 打开剪贴板历史 |
| Alt + Shift + Z | 打开截图工具 |
| Esc | 取消当前截图 |

## 注意事项

- OCR 与翻译功能支持两种后端：本地 ONNX 模型和腾讯云 API
- 使用腾讯云 API 需在设置页配置 SecretId 和 SecretKey
- 本地模型不随仓库提交，需按说明下载并配置模型目录
- 首次启动会尝试注册 Windows 文件右键菜单
- 全局快捷键可能受安全软件或权限策略影响
